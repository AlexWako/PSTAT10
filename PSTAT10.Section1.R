# Installing Packages
install.packages('base')
install.packages('datasets')
install.packages('graphics')
install.packages('stats')

# Loading Packages
library(base)
library(datasets)
library(graphics)
library(stats)

# Arithmetic operators can be used using
# + for addition
# - for subtraction
# * for multiplication
# / for division 
8/2-2*(2-3)

# Square root can also be done by sqrt()
sqrt(1024)

#3 Creating an object
NewObject <- "First Lab Section" 
# The object 'NewObject' is now the string "First Lab Section"
